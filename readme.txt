In BaseTest class update the desired capabilities depends on which device the testcase
Going to execute.
Before executing test code install applium server and keep appium on running mode
by typing command as appium
Check the device connectivity with the system with adb devices command
Execute the testng.xml file to verify the testcodes.